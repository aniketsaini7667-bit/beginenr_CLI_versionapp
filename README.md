# beginenr_CLI_versionapp
hello, readers 
<br>
So this repo is a basically a CLI version of a note taking python code and it will save the note in a file on your device and can also print the note in a good format by only CLI format
